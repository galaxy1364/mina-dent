# UI KIT v2 (MinaDent) — iOS26+
- Goals: iOS-first, smooth motion, zero-lag, WCAG2.2AA, RTL-ready
- Components: Button, Input, Card, Sheet/Modal (state-machine), Tabs, Toast, Skeleton
- Motion: meaningful micro-interactions (no shake), 120ms–240ms, prefers-reduced-motion respected
- Layout: safe-area aware, stable scroll, no dead taps, no overflow-jank
